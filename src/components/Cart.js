import React from 'react';
import '../styles/Cart.css'; // Correctly reference the CSS file in the styles folder

const Cart = () => {
    return (
        <div className="cart">
            <h2>Your Cart</h2>
            <p>No items in cart.</p>
        </div>
    );
};

export default Cart;
